{
    var comp = 10;
    var alt = 5;
    var larg = 5;
    var volume = function (comprimento, altura, largura) { return (comprimento * altura * largura); };
    console.log("A media \u00E9 " + volume(comp, alt, larg));
}
